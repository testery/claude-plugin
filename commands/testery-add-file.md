---
description: Attach a file (artifact, log, input) to an existing Testery test run.
---

Use the `testery-add-file` skill (`testery add-file`). The file path is a positional argument (`testery add-file <FILE_PATH> --test-run-id <id> [--kind <KIND>]`).

User input: $ARGUMENTS
