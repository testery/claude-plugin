---
description: Open the Playwright HTML report from the most recent local run in a browser.
---

Use the `testery-playwright-bdd-view-report` skill. Run `npx playwright show-report` in the background so the local server stays up; it auto-opens the default browser.

User input (optional: an alternate report path): $ARGUMENTS
