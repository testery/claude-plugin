---
description: List recent Testery test runs, optionally filtered (branch, status, environment, project, name, phase).
---

Use the `testery-list-test-runs` skill (`testery list-test-runs`). Supports `--limit` (1-50) and repeatable `--filter Name=<field>,Values=<...>`.

User input: $ARGUMENTS
